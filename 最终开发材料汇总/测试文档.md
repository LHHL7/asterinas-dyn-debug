# Asterinas Dynamic Debug System 测试文档

## 1. 文档目标

本文档为 Asterinas Dynamic Debug System 的**最终测试报告**，覆盖以下验证维度：

1. 功能正确性（F 系列）
2. 增量刷新链长解耦（I-03）与索引消融（I-02）
3. 性能基准 —— 微基准 + 真实 workload（P 系列，含 P-01R）
4. Batch Patch Transaction 效果（P-02）
5. SMP 并发稳定性（C 系列）
6. 系统边界与已知限制

本文档同时作为：

- 测试执行指南（编译命令 + 测试命令）
- Benchmark 执行规范（参数约束 + 判定标准）

结构化测试数据存放于 `results/` 目录，详见第 9 节。

---

## 2. 测试环境

### 2.1 硬件环境

| 项目 | 内容 |
| --- | --- |
| 设备 | 远程服务器（Docker 容器 `asterinas-dev`） |
| CPU | Intel(R) Core(TM) i9-7900X @ 3.30GHz（10 核 20 线程，1 Socket） |
| 内存 | 46 GiB（Docker 可用 46.7 GiB） |
| 存储 | 222 GB NVMe SSD |
| 系统 | Ubuntu 24.04.4 LTS，kernel 6.14.0-27-generic |

### 2.2 虚拟化环境

| 项目 | 内容 |
| --- | --- |
| QEMU 版本 | 通过 `make run_kernel`（Docker 容器），版本待补 |
| 内存 | 16G（`MEM=16G`） |
| 编译模式 | release（`RELEASE=1`） |

**通用固定参数**：`LOG_LEVEL=debug SYSCALL_INFO=off`。

- `LOG_LEVEL=debug`：必须，`dyndbg_debug!` 走 `log::debug!`，低于此级别不会触发日志输出。
- `SYSCALL_INFO=off`：关闭 `info!` 级别的 syscall 入口/出口追踪（`kernel/src/syscall/mod.rs`），避免每次系统调用产生海量 `info` 日志污染串口、干扰 benchmark 计时。**不影响 `dyndbg_debug!`（走 `debug` 级别）。**

**SMP 和 RELEASE 按测试类别区分**：

| 测试系列 | SMP | RELEASE | 原因 |
|----------|-----|---------|------|
| F（功能） | 1（默认） | 1 | 单线程串行，release 即可 |
| I-03（增量刷新） | 1（默认） | 1 | 使用 TSC 计时 + 原子计数器，无 debug 断言依赖 |
| I-02（索引消融） | 1（默认） | 1 | 使用 uptime 计时 + 原子计数器，无 debug 断言依赖 |
| P（性能） | **1** | 1 | 单线程基准，多核引入调度抖动干扰计时 |
| C（并发） | **4** | 1 | 多核并发 patching 是 C 系列的核心测试目标 |

**同构建分组**：以下用例共用同一编译命令，可通过 `/test/dyndbg/run_all.sh` 一键执行：

```bash
make run_kernel LOG_LEVEL=debug SYSCALL_INFO=off RELEASE=1 MEM=16G
# guest 内：
/test/dyndbg/run_all.sh
```

| 用例 | 测试内容 |
|------|---------|
| F-01~F-08 | 功能正确性 |
| I-02 | 多维度索引消融 |
| P-01（static） | 微基准 static path |
| P-01R（static） | 真实 workload static path |
| P-02 | batch patch 开销 |

**需单独编译启动的用例**：

| 用例 | 编译命令差异 |
|------|------------|
| P-01（baseline） | `NO_DEFAULT_FEATURES=1 FEATURES=cvm_guest` |
| P-01（branch） | `NO_DEFAULT_FEATURES=1 FEATURES=cvm_guest,branchdbg` |
| P-01R（baseline/branch） | 同上 |
| C-01~C-03 | `SMP=4` |

`LOG_LEVEL=debug` 对 I 系列非强制——I 系列主要读取 `/proc/sys/kernel/dyndbg_stats` 计数器与 TSC 延迟，以 `warn` 级别运行可压低 DEBUG 噪声、减少串口阻塞，不影响统计结果。

测试结果收集说明：`tools/dyndbg/*.sh` 会被打包进 initramfs，guest 启动后可直接在 shell 里执行 `/test/dyndbg/*.sh`。当前内核不支持 9P，因此结果会优先尝试写到 `/results`，失败后回退到 `/ext2/results`。测试结束后，使用 `tools/dyndbg/collect_results.sh` 把 ext2 镜像里的结果同步回宿主机的 `results/` 目录。

同步顺序：guest 里先执行 `sync`，再 `umount /ext2`，最后在宿主机单独开一个终端运行 `sh tools/dyndbg/collect_results.sh`。否则 loop-mount 可能看不到刚写入的 `results/`。收集脚本会自动把旧 CSV 表头重写成统一的新 schema，避免宿主 `results.csv` 残留旧标题。

宿主机收集后的结果会落到源码树下的 `results/` 目录。

收集结果的示例：

```bash
make run_kernel LOG_LEVEL=debug SYSCALL_INFO=off RELEASE=1 MEM=16G

# 同步结果
sync；umount /ext2

# guest 测试结束并卸载 ext2 后，把 ext2 镜像里的结果收集回宿主机 results/
sh tools/dyndbg/collect_results.sh
```

说明：正式测试在上述远程服务器 Docker 容器（`asterinas-dev`）内执行。

### 2.3 编译环境

| 项目 | 内容 |
| --- | --- |
| Rust 版本 | nightly-2025-12-06（见 `rust-toolchain.toml`） |
| 编译模式 | release（`RELEASE=1`），I 系列除外 |
| 内存 | `MEM=16G` |

所有测试通过 `make run_kernel` 构建并启动 QEMU guest，无需单独 `cargo build`。

**三组 P 系列编译命令**：

```bash
# baseline（dyndbg 编译期消除）
make run_kernel LOG_LEVEL=debug SYSCALL_INFO=off RELEASE=1 MEM=16G \
  NO_DEFAULT_FEATURES=1 FEATURES=”cvm_guest”

# branch（branch gate，无 static patch）
make run_kernel LOG_LEVEL=debug SYSCALL_INFO=off RELEASE=1 MEM=16G \
  NO_DEFAULT_FEATURES=1 FEATURES=”cvm_guest,branchdbg”

# static（static patch 站点存在，运行时 -p 禁用）
make run_kernel LOG_LEVEL=debug SYSCALL_INFO=off RELEASE=1 MEM=16G
```

说明：`NO_DEFAULT_FEATURES=1` 关闭默认 features（含 `dyndbg`），再通过 `FEATURES` 精确指定要启用的 feature。static build 不传额外参数，走默认 `cvm_guest + dyndbg`。

---

## 3. 当前实现语义说明（必读）

### 3.1 命令语法

selectors 在前，action 在最后：

```text
module=mm +p
file=mm/ func=alloc_ +p
line=123 -p
module=dyndbg_bench +trace
file=open.rs +pfl
file=open.rs -f
func=bench_log =fl
```

三类命令：

| 命令格式 | 语义 |
|----------|------|
| `<selectors> <action>` | 追加一条规则（log/trace 启停 + 格式标志） |
| `del <id>` | 按索引删除规则（触发全量重算） |
| `clear` | 清空全部规则（触发全量重算） |

action 语法（Linux 风格单 token）：

| 动作 token | 含义 |
|-----------|------|
| `+p` / `-p` | 启用/禁用日志输出（log 维度，last-match-wins） |
| `+trace` / `-trace` | 启用/禁用追踪事件（trace 维度，与 log 独立） |
| `+f` / `+l` / `+m` / `+t` | 追加函数名 / file:line / 模块路径 / 线程 ID 前缀 |
| `-f` / `-l` / `-m` / `-t` | 移除对应前缀 |
| `=fl` 等 `=` 操作 | 覆盖：将格式标志替换为指定集合 |
| `+_` / `=_` | 清空全部格式标志 |
| `+pfl` 组合 | 开关 + 标志一次指定（如 `+pfl` = 启用日志 + 函数名/行号前缀） |

仅含标志的规则（如 `+f`）不改变 log/trace 开关，仅调整格式标志（KeepState 动作）。

### 3.2 Selector 语义

四维选择器（file / module / func / line）之间为 AND 语义，keyword 匹配采用**三通道匹配**：

1. **完整值精确**：`module=dyndbg_bench::bench_sites` 精确命中（主索引 O(log N) 点查）
2. **段精确**：module 按 `::` 切段、file 按 `/` 切段——短名（`module=dyndbg_bench`）或多段组合（`file=fs/ext2`）命中段倒排索引
3. **通配符**：`*` / `?` 通配符扫描主索引键（`module=*ndbg*`）

function 维度是原子值、无段概念，退化为"精确 → 通配符"两通道；line 为精确行号。

可用示例：

```text
file=mm/            # 段精确（路径含 mm 段）
func=alloc_         # 通配符语义（_ 为普通字符，* 为通配符）
module=*ndbg*       # 通配符
file=fs/ext2        # 多段交集
```

### 3.3 module selector 语义

module 匹配的是 Rust 的 module_path，而不是 log target。

### 3.4 Rule Chain 语义

- 多规则参与匹配
- 最终由最后命中规则决定（last-match-wins）

### 3.5 Static Patch 语义

- NOP5 <-> JMP rel32
- disabled fast path 为纯 NOP 流

### 3.6 架构假设

- 当前只验证 x86_64
- 依赖 x86 I-Cache coherence
- 其他架构未实现显式 I-Cache flush

### 3.7 已知限制

- module id 数量有限
- 超限后进入 UNASSIGNED_MODULE_ID，对应日志点永久禁用
- 未覆盖项：非 x86 I-Cache flush、CPU hotplug、模块卸载/重载、嵌套 patch 事务

---

## 3.8 测试用例清单（Case List）

说明：以下用例为测试执行的主清单，执行时可在各小节内补充更细化步骤。

| Case ID | 目标 | 输入/操作 | 预期结果 | 备注 |
| --- | --- | --- | --- | --- |
| F-01 | Module selector 基础匹配 | module=mm +p | 仅 module_path 含 mm 段的日志输出 | 段精确（module 按 :: 切段） |
| F-02 | File selector 段匹配 | file=mm/ +p | 仅 file 路径含 mm 段的输出 | 段精确（file 按 / 切段） |
| F-03 | Function selector 匹配 | func=bench_log +p | 仅函数名与 func 关键字精确匹配的输出 | 函数名为 module_path::fn，原子值无段概念 |
| F-04 | Line selector 精确匹配 | line=123 +p | 仅行号 123 输出 | 行号必须存在 |
| F-05 | 规则冲突 last-match-wins | module=mm +p; func=alloc -p | alloc 不输出，其他 mm 输出 | 交换顺序验证覆盖 |
| F-06 | 动态切换即时性 | clear; module=mm +p; clear | enable 立即生效，clear 立即失效 | 避免规则叠加 |
| F-07 | 非法输入鲁棒性 | ++++; line=abc; func=== | 返回错误且不崩溃 | 记录 errno |
| F-08 | 多层规则覆盖 | module=mm +p; file=mm/ +p; func=alloc -p; line=123 +p | last-match-wins 生效 | 多层冲突覆盖 |
| FL-01~08 | 输出格式 flags | `+pfl` / `+f` / `-f` / `=fl` / `+_` / `+pt` / 多规则累积 | 前缀按规则链顺序模拟（set → clear → override） | 见 4.9.1 |
| M-01~08 | 三通道匹配语义 | 精确 / 段 / 通配符 / 部分名不匹配 / func 原子语义 / 索引 on/off 等价 | 三通道语义正确且 index=on/off 候选集一致 | 见 4.9.2 |
| R-01~06 | 异常命令鲁棒性 | 非法动作 / 非法 flags / 越界 del / 超长 / 空命令 | 不改变规则链、不 panic | 见 4.9.3 |
| S-01~07 | 状态查看 | cat 规则链与全部调试点状态 | 状态列 +p/+trace、del/clear 后重放一致 | 见 4.9.4 |
| T-01~06 / H-01~04 | 追踪与热点 | +trace 事件计数 / 丢失统计 / per-CPU 归属 / 热点 top-n | ring 事件、丢失数、热点计数一致 | 见 4.9.5 |
| I-03 | 增量刷新链长解耦 | 构造链长 1..5000，append 与 del 分别计时 | append 恒定 ~2µs 与链长无关；del 随链长线性（17→58,363µs）；full 基线恒定 65µs | 替代已废弃的 I-01（见 5.1 历史注记） |
| EQ-01~EQ-04 | 增量与全量状态等价 | append/flags-only/del 场景下对比增量刷新与全量重放 | 两条路径的最终描述符状态完全一致 | 见 5.1.2 |
| I-02 | 多维度索引消融 | 同一内核 index=on/off 对比，line/file/func/module 四维度 `-p` | index=on 显著快于 index=off：line 2.82×、file 8.40×、func 4.66×、module 5.60×（283 描述符）；可扩展性实验 N=500..10000 下精确查找类放大至 118-307× | 使用 `-p` 消除 patching 噪声，仅测量候选收集路径 |
| P-01 | Static fast path（微基准） | dyndbg_bench 紧凑循环，三构建对比 | `baseline ≈ static < branch` | TSC 计时，禁止串口输出 |
| P-01R | Static fast path（真实 workload） | 6 类 syscall 场景，三构建对比 | `baseline ≈ static < branch` | 覆盖 FS/IPC/进程/fd 子系统 |
| P-02 | Batch patch 开销 | 同一内核下 per-site vs batch | batch patch 更快 | 单次脚本自动跑两轮 |
| C-01 | 并发 patch 稳定性 | 规则切换并发（module=$MODULE_KEY +p） | 无 panic/crash/死锁 | 验证规则切换稳定性 |
| C-02 | 高频 patch 压测 | 1e5 次 enable/disable | 系统稳定 | 记录耗时 |
| C-03 | Patch Storm（真实场景混合负载） | 多 CPU 混合规则操作，默认 `BENCH_MODE=log` | 无死锁/非法指令/崩溃 | 覆盖真实打印路径 |

Case 与章节对应关系：

| Case ID | 对应小节 |
| --- | --- |
| F-01 | 4.1 Module Selector 测试 |
| F-02 | 4.2 File Selector 测试 |
| F-03 | 4.3 Function Selector 测试 |
| F-04 | 4.4 Line Selector 测试 |
| F-05 | 4.5 Rule Chain 冲突测试 |
| F-06 | 4.6 动态 Enable/Disable 测试 |
| F-07 | 4.7 非法输入鲁棒性测试 |
| F-08 | 4.8 多层规则覆盖测试 |
| I-03 | 5.1 增量刷新链长解耦 |
| EQ-01~EQ-04 | 5.1.2 增量与全量状态等价 |
| I-02 | 5.2 多维度索引消融 |
| P-01 | 6.1 Disabled Fast Path Benchmark |
| P-01R | 6.1.1 Real Workload Supplement |
| P-02 | 6.2 Batch Patch Transaction Benchmark |
| C-01 | 7.1 规则切换并发 |
| C-02 | 7.2 高频 Patch Stress |
| C-03 | 7.3 Patch Storm |
| FL-01~08 | 4.9.1 输出格式 flags |
| M-01~08 | 4.9.2 三通道匹配语义 |
| R-01~06 | 4.9.3 异常命令鲁棒性 |
| S-01~07 | 4.9.4 状态查看 |
| T/H-01~04 | 4.9.5 追踪与热点 |

---

## 4. 功能正确性测试

F 系列与 P 系列 static 共用同一 build（默认 features，dyndbg 启用）：

```bash
make run_kernel LOG_LEVEL=debug SYSCALL_INFO=off RELEASE=1 MEM=16G
```

F 系列主要是验证 selector 语义和规则链行为，不是性能压测；因此正式执行时不需要很大的 `ITERS`。默认使用 `ITERS=100`，既能覆盖每个测例的规则操作和日志输出链路，又不会让验证过程过长。

测试脚本：`/test/dyndbg/functional.sh`

F-04 与 F-08 的 `line` 选择器由 initramfs 构建时生成的 `/etc/dyndbg_line.txt` 提供；脚本运行时优先读取该文件，读不到时回退到稳定默认值。这样即使 guest 内没有源码树，也能保持 line 选择稳定。

快速参数档：

```bash
# 1) guest 中运行功能测试快速档
ITERS=10 /test/dyndbg/functional.sh
```

正式命令：

```bash
# guest 中运行功能测试正式档
/test/dyndbg/functional.sh


# 需要手工指定 line 时可覆盖：
# LINE_KEY=<new_line> ITERS=100 /test/dyndbg/functional.sh
```

### 4.1 Module Selector 测试 (F-01)

**目标**：验证 module selector 是否正确控制目标日志。

**步骤**：

```text
module=mm +p
```

**预期**：
- 目标 descriptor 被命中并通过 bench 判定为 enabled
- 其他模块不受该 selector 影响


---

### 4.2 File Selector 测试 (F-02)

**步骤**：

```text
file=mm/ +p
```

**预期**：
- 目标 descriptor 被命中并通过 bench 判定为 enabled
- file selector 对应的目标范围生效


---

### 4.3 Function Selector 测试 (F-03)

**步骤**：

```text
func=alloc_ +p
```

**预期**：
- 目标 descriptor 被命中并通过 bench 判定为 enabled
- function selector 对应的目标范围生效


---

### 4.4 Line Selector 测试 (F-04)

**步骤**：

```text
line=123 +p
```

**预期**：
- 目标 descriptor 被命中并通过 bench 判定为 enabled
- line selector 对应的精确行号生效


---

### 4.5 Rule Chain 冲突测试（last-match-wins）(F-05)

**步骤**：

```text
module=mm +p
func=alloc -p
```

**预期**：
- 通过 bench 的 enabled/disabled 状态验证 last-match-wins 规则
- 交换顺序后，最终命中的规则决定结果

交换顺序验证覆盖关系。


---

### 4.6 动态 Enable/Disable 测试 (F-06)

**预期**：
- enable 后 bench 判定为 enabled
- clear 后 bench 判定为 disabled
- 连续切换时规则状态与 bench 结果保持一致


---

### 4.7 非法输入鲁棒性测试 (F-07)

**测试输入**：

```text
++++
line=abc
func===
+ ???
```

**预期**：
- 非法输入不会新增规则
- 不 panic、不 crash


---

### 4.8 多层规则覆盖测试 (F-08)

**测试目标**：验证多层规则下 last-match-wins 语义稳定。

**步骤**：

```text
module=mm +p
file=mm/ +p
func=alloc -p
line=123 +p
```

**预期**：
- 通过 bench 状态验证最终规则覆盖结果
- last-match-wins 在多层规则下保持稳定

---

### 4.9 格式标志、匹配语义与观测接口测试

针对输出格式 flags、三通道匹配、鲁棒性、状态查看、追踪观测等能力，以独立脚本逐一验证（共 44 个用例，全部通过）。各脚本共享 `PROC=/proc/sys/kernel/dynamic_debug`、`STATS=/proc/sys/kernel/dyndbg_stats`、`BENCH=/proc/sys/kernel/dyndbg_bench` 三个 procfs 路径。

#### 4.9.1 输出格式 flags (FL-01~08)

脚本：`/test/dyndbg/flags.sh` —— 验证 `+f/+l/+m/+t` 前缀与 flags 操作的顺序语义（见 3.1）：

| 用例 | 测试内容 | 验证逻辑 |
|------|----------|---------|
| FL-01 | `+pfl` 前缀组合 | 输出含 file:line、[module]、function 三段前缀 |
| FL-02 | flags-only `+f` | 仅 `+f`（无 `+p`）不改变 log 开关，且不产生日志输出 |
| FL-03 | `-f` 清除单标志 | `+pfl` 后追加 `-f`：function 前缀消失，file:line 与 module 保留 |
| FL-04 | `=fl` 覆盖 | `+pflm` 后追加 `=fl`：标志被覆盖为仅 f+l |
| FL-05 | `+_` 清空全部标志 | `+pfl` 后追加 `+_`：全部前缀清除，输出为裸消息 |
| FL-06 | `+pt` 线程前缀 | 输出含 `[task=0x...]` 线程指针前缀 |
| FL-07 | 多规则标志累积 | 两条规则分别设置不同标志，验证标志按链顺序累积 |
| FL-08 | 无标志的裸消息 | 仅 `+p` 无标志时输出无前缀 |

#### 4.9.2 三通道匹配语义 (M-01~08)

脚本：`/test/dyndbg/match3.sh` —— 验证精确/段/通配符语义与索引等价性：

| 用例 | 测试内容 | 验证逻辑 |
|------|----------|---------|
| M-01 | 完整值精确 | `module=dyndbg_bench::bench_sites` 精确命中指定模块，不波及 bench_log |
| M-02 | 祖先段命中 | `module=dyndbg_bench`（祖先段）命中全部 65 个站点 |
| M-03 | file 段命中 | `file=bench_sites.rs` 段精确命中该文件站点 |
| M-04 | 通配符 `*ndbg*` | 通配符命中 |
| M-05 | 部分名不匹配 | `module=ndbg` / `file=ench_sites`（非完整值、非完整段）不匹配——仅精确与段命中 |
| M-06 | func 原子语义 | 短名精确命中、部分名不命中、通配符命中 |
| M-07 | 索引 on/off 等价 | index=on 与 index=off 下候选集一致（m=65/65、f=64/64、w=65/65） |
| M-08 | AND 收窄 | module + file AND 收窄到 bench_sites |

#### 4.9.3 异常命令鲁棒性 (R-01~06)

脚本：`/test/dyndbg/robustness.sh` —— 验证异常命令不破坏规则链：

| 用例 | 测试内容 | 验证逻辑 |
|------|----------|---------|
| R-01~R-05 | 非法动作/非法 flags/越界 del/超长命令/空命令 | 断言 `before_rules == after_rules` |
| R-06 | 出错后系统健康 | 错误注入后正常规则仍生效，系统未受污染 |

#### 4.9.4 状态查看 (S-01~07)

脚本：`/test/dyndbg/status.sh` —— 验证 `cat` 规则链与调试点状态列表：

| 用例 | 测试内容 | 验证逻辑 |
|------|----------|---------|
| S-01 | clear 后初始状态 | `rules=0`、全部 283 个描述符 `-p` |
| S-02 | 规则链带索引输出 | 追加两条规则后 `rules=2` 且带索引输出 |
| S-03/S-04 | 状态列 +p / +trace | 模块级 `+p` 后 65 个站点状态列全部为 `+p`；`+trace` 同理 |
| S-05/S-06 | del 后重放重置/翻转 | `del` 后状态回归默认或翻回 |
| S-07 | clear 状态重置 | 全部站点回归 `-p` |

#### 4.9.5 追踪与热点 (T-01~06 / H-01~04)

脚本：`/test/dyndbg/trace.sh` —— 验证 trace 数据通路（per-CPU ring buffer）：

| 用例 | 测试内容 | 验证逻辑 |
|------|----------|---------|
| T-01/T-02 | trace 启用/禁用计数 | `+trace` 后事件数 = 站点命中数；禁用后为 0 |
| T-03/T-03b | log/trace 维度独立 | 仅 `+trace` 时 log 禁用而事件正常；仅 `+p` 时不产生事件 |
| T-04 | reset 清零 | 事件数、丢失数、热点行均清零 |
| T-05 | ring 溢出丢失统计 | 100 万次事件注入后 `lost=998976`（容量 1024/CPU） |
| T-06 | per-CPU 归属 | 事件全部归属合法 CPU（`bad_cpu=0`） |
| H-01/H-02 | 热点 top-1 / 一致性 | 热点 top-1 为 dyndbg_bench.rs 且 count ≥ 10000；热点计数与事件计数一致 |
| H-03/H-04 | 热点 reset / top-10 | reset 清零；log_batch 多站点模式填满 top-10 |


## 5. 增量更新测试

规则链存在两条状态刷新路径：**增量刷新**（append 专用，不重跑规则链）与**全量重放**（del/clear 专用）。本测试验证两条路径的延迟特性与状态等价性。使用 RELEASE=1 构建（与其余系列一致），计时读取 `last_update_latency_us`（TSC）。

```bash
make run_kernel LOG_LEVEL=debug SYSCALL_INFO=off RELEASE=1 MEM=16G
```

### 5.1 增量刷新链长解耦 (I-03)

**目标**：验证 append（增量刷新）的更新延迟与规则链长度无关（O(k)），del（全量重放）随链长线性增长（O(L·k)）。

> 历史注记：此前的 I-01 用例（以"module 规则 vs 无选择器规则"对比重算规模）已废弃——其命中集混淆设计在增量刷新路径引入后被替换；本套件以 I-03 链长解耦 + EQ 等价性取代，见 `tools/dyndbg/incremental.sh` 头部说明。

**前置要求**：`/proc/sys/kernel/dyndbg_stats` 提供 `last_update_latency_us`（最近一次规则更新的 TSC 延迟，微秒）与 `descriptors_recomputed`。

**步骤**：
1. 构造规则链长度 1..5000（`CHAIN_LENS`，默认 "1 10 100 1000 5000"）
2. 每个链长下分别测量：
   - **append 延迟**：向链尾追加 `module=dyndbg_bench +p`，走增量刷新路径（只重算新规则命中的 65 个描述符）
   - **del 延迟**：删除链首规则（`del 0`），走全量重放路径（对受影响描述符重放完整规则链）
   - **full 基线**：无选择器 `+p` 规则，重放整条链并重算全部 283 个描述符
3. 每次测量前 `echo reset > /proc/sys/kernel/dyndbg_stats`，从 `last_update_latency_us` 读取耗时

测试脚本：`/test/dyndbg/incremental.sh`

```bash
# 直接运行即可，不需要额外参数
/test/dyndbg/incremental.sh
```

**预期**（实测结果）：

| 规则链长度 | append 延迟 (µs) | del 延迟 (µs) | full 基线 (µs) |
|-----------|-----------------|--------------|----------------|
| 1 | 2 | 17 | 65 |
| 10 | 2 | 118 | 65 |
| 100 | 2 | 1,158 | 65 |
| 1,000 | 3 | 11,749 | 65 |
| 5,000 | 2 | 58,363 | 65 |

- **append 恒定 ~2µs**，与链长完全无关——增量刷新只对新规则命中集做增量变换，O(k) 复杂度
- **del 随链长线性增长**（17µs → 58,363µs）——删除改变"最后命中者"，必须重放整条链 O(L·k)
- **full 基线恒定 65µs**——只与描述符总数（283）相关，与链长无关

#### 5.1.2 增量与全量状态等价 (EQ-01~EQ-04)

`incremental.sh` 的 EQ 用例验证增量刷新与全量重放的最终描述符状态一致：

| 用例 | 测试内容 | 验证逻辑 |
|------|----------|---------|
| EQ-01 | append last-match-wins | 追加覆盖规则后按 last-match-wins 生效 |
| EQ-02 | flags-only 保持开关 | flags-only 规则不改变 log/trace 开关 |
| EQ-03 | del 恢复 +p | `del` 删除禁用规则后状态恢复 `+p` |
| EQ-04 | 增量 == 全量 | 增量刷新路径（append）与全量重放路径的最终描述符状态完全一致 |

EQ-04 通过 `dyndbg_bench` 的 `recompute=incremental|full` 开关对比两条路径（`echo "recompute=full" > /proc/sys/kernel/dyndbg_bench`），读取 `descriptors_recomputed` 验证等价性。

### 5.2 多维度索引消融 (I-02)

**目标**：通过关闭索引（退化为线性扫描），测量多维度索引对候选收集阶段的加速贡献。

**原理**：
- 系统维护 file/module/function/line 四维主索引（BTreeMap 精确键）+ 两棵段倒排索引（module 按 `::`、file 按 `/` 切段），规则更新时通过三通道索引定位受影响 descriptor 集合
- 通过 `/proc/sys/kernel/dyndbg_bench` 的 `index=on|off` 开关，可将候选收集切换为全量 descriptor 线性扫描
- I-02 测量的是"候选收集"阶段的纯粹差异，而非 descriptor 重算粒度
- 使用 `-p`（disable）替代 `+p`，消除静态修补（NOP↔JMP）噪声：所有 descriptor 默认禁用，`-p` 保持禁用态，不触发状态迁移，无指令修补

**索引实现细节**：
- 四维主索引均为 BTreeMap 精确键，**真正 O(log N) 点查**：`line_index: BTreeMap<u32, Vec>`、`file/module/function_index: BTreeMap<&str, Vec>`
- 两棵段倒排索引：`module_segment_index`（module_path 按 `::` 切段）、`file_segment_index`（file 按 `/` 切段），服务于段精确通道
- 三通道候选收集（`collect_by_keyword_exact_first`）：完整值精确（主索引 O(log N)）→ 段精确（段倒排逐段查表 + 交集 O(k·log N)）→ 通配符扫描（主索引键 `match_wildcard`，O(m)）；function 维度为"精确 → 通配符"两通道
- 消融路径（`index=off`）：`all_descriptors().filter(rule.matches_descriptor)`，复杂度 O(all_descriptors)

**前置要求**：
- 内核编译时包含 `dyndbg` feature（默认）
- `/proc/sys/kernel/dyndbg_bench` 支持 `index=on|off` 控制
- `dyndbg_bench` 模块包含 65 个 descriptor，覆盖 four selector 维度

**步骤**：
1. 以默认 `LOG_LEVEL=debug` 编译启动（`-p` 不触发日志输出，无干扰）
2. guest 内执行 `/test/dyndbg/index_ablation.sh`
3. 脚本自动在 `index=on` 和 `index=off` 下各跑四组 selector：`line=N -p`、`file=dyndbg_bench.rs -p`、`func=bench_log_0 -p`、`module=dyndbg_bench -p`，每轮 `INDEX_ITERS` 次（默认 10000）
4. 每次迭代执行 `selector -p` + `clear`，规则从空状态开始，清除累积效应

**预期**（283 描述符，10 轮 × 1000 次规则更新取均值）：

| 选择器 | index=on (s) | index=off (s) | 加速比 |
|--------|-------------|---------------|--------|
| line | 0.018 | 0.051 | **2.82×** |
| file | 0.020 | 0.164 | **8.40×** |
| func | 0.011 | 0.051 | 4.66× |
| module | 0.020 | 0.114 | **5.60×** |

- `sites_patched` 全部为 0，确认无 patching 噪声混入
- `descriptors_recomputed` 在 index=on 和 index=off 下相等，确认工作负载一致
- 四个维度全部享受索引加速（file 最高 8.40×），区别于早期"仅 line 维度索引"的旧设计
- 全量重算基线（无选择器规则，`recompute=full`）0.091s，高于任何 index=on 路径

**三模式分解**（同一条 `module=dyndbg_bench -p` 规则）：

| 模式 | 耗时 (s) | 相对 L0 |
|------|---------|---------|
| L0: 全量重算 | 0.091 ± 0.013 | 1.00× |
| L1: 增量 + O(n) 扫描 | 0.114 ± 0.002 | 1.25× |
| L2: 索引驱动增量 | 0.0203 ± 0.0001 | 0.22× |

L2 较 L1 缩短 82%（5.6× 加速）——三通道索引将候选收集从 O(n) 降至 O(k)；无索引的增量重算（L1）劣于朴素全量重算（L0），只有索引将候选集收窄后两阶段架构才产生净收益。

**可扩展性**（`scalability.sh`，合成站点 N=500~10,000，N=283 为真实内核规模）：

- **(a) file 键扫描类**：加速比平坦 3.4×–8.4×（N=10,000 时 3.36×），收益来自段精确命中的 O(log N) 查表，与候选集规模无关
- **(b) 精确查找类（line/func/module）**：加速比随 N 线性放大——line 8.57×→**118×**、func 10.5×→**141×**、module 21.5×→**307×**（N=500→10,000）；index=on 为 O(log N) 恒定查表，index=off 的线性扫描 O(N) 随 N 增长

**使用脚本**：`/test/dyndbg/index_ablation.sh`（消融）、`/test/dyndbg/scalability.sh`（可扩展性）

```bash
# 快速参数档：
INDEX_ITERS=1000 /test/dyndbg/index_ablation.sh
# 正式档：
/test/dyndbg/index_ablation.sh
```

结果 CSV 路径：`results/index_ablation/results.csv`（消融）、`results/scalability/N{500..10000}/index_ablation/results.csv`（可扩展性，按 N 分目录）


---

## 6. 性能测试

注意：性能测试禁止真实串口输出，否则 IO 开销会掩盖 fast path 结果。
日志输出经 ring buffer 落地到 dmesg，不经过串行控制台。

### 6.0 Benchmark 约束与判定标准

固定条件：

- 固定 QEMU 参数与 CPU 核数
- release 构建
- 固定迭代次数，warmup 1 次
- 每项至少 3 轮取均值

三向对比的执行原则：

- baseline、branch、patch 必须分别编译、分别启动、分别采样
- 同一次 guest 启动只测一个 backend，不在脚本内部自动切换 backend
- `perf.sh` 通过 `BACKEND_MODE=baseline|branch|static` 指定当前启动对应的 backend
- `workload.sh` 通过 `WORKLOAD_MODE=baseline|branch|static` 指定当前启动对应的 workload 采样模式

说明：baseline、branch、patch 这三组不是在同一次 guest 里切 backend，而是要分别编译、分别启动三次，再分别执行测试命令。

判定标准：

- static path cycles 不高于 baseline 的 1.1x
- static patch 相比 descriptor 应降低 branches/branch-misses
- 若趋势相反，标记为 review 并记录原因

#### 三组构建的消融关系

P 系列的三组构建并非三种并列的编译配置，而是构成一条干净的**模块消融链**（module-level ablation chain）。其中 branch 的定位不是"另一个功能选项"，而是**为了量化静态指令修补优化贡献而刻意保留的系统中间态**。

三组构建包含的组件差异：

| 构建 | 描述符 + 静态注册 | 规则链 + 索引 + 增量重算 | 模块级门控 | Static Patch（NOP↔JMP） | 批量修补事务 |
|------|:---:|:---:|:---:|:---:|:---:|
| baseline | | | | | |
| branch | ✅ | ✅ | ✅ | | |
| static (patch build) | ✅ | ✅ | ✅ | ✅ | ✅ |

三组构建的角色定位：

| 构建 | 消融角色 |
|------|---------|
| baseline | **绝对下限**：编译期完全消除，所有调用点直接消失 |
| branch | **静态修补优化的消融对照**：保留描述符 + 运行时过滤引擎 + 模块门控，但用软件分支判断替代指令修补。与 static 的唯一差异是 static patch 的有无 |
| static（patch build） | **最终实现的性能基线**：完整系统，static patch 站点运行时常驻但被规则禁用 |

消融链：

```
baseline ──(+描述符+过滤引擎+模块门控)──→ branch ──(+static patch+批量修补)──→ static
   ↑                                         ↑                                    ↑
   编译期消除                           软件分支判断                        硬件 NOP/JMP 短路
```

核心判据：

- `baseline ≈ static < branch` 成立 → static patch 有效消除了分支判断开销，将禁用路径压至接近编译期消除
- `static - branch` 的差值是 **static patch + 批量修补的整体贡献**（模块门控在两组中均存在，已从差值中抵消）
- `branch - baseline` 的差值是**描述符注册 + 运行时过滤引擎 + 模块门控的固有开销**（无硬件指令修补时的纯软件路径成本）

### 6.1 Disabled Fast Path Benchmark (P-01)


指标：elapsed（必选）+ task_clock_ms / context_switches / page_faults（软指标）+ cycles / instructions / branches / branch-misses（硬件 PMU，可选）

说明（当前仓库与内核能力）：

- `cycles/instructions/branches/branch-misses` 依赖 `perf stat` 与 `perf_event_open` 路径。
- 当前 Asterinas 兼容性文档标注 `perf_event_open` 为不支持项，因此在 guest 内通常无法稳定采集硬件 PMU 计数。
- `tools/dyndbg/perf.sh` 已支持在无 `perf` 场景下退化采集软指标：`task_clock_ms`（基于 `/proc/self/stat` 的 utime+stime）、`context_switches`（`/proc/stat` 的 `ctxt` 差值）、`page_faults`（`/proc/stat` 的 `page_faults` 差值）。
- `RESULT` 和 CSV 只保留软指标列：`elapsed_ms`、`task_clock_ms`、`context_switches`、`page_faults`。

与题目要求的对应关系：

- 性能基线：`mode=log` 的 static path 作为动态禁用下的主基线；真正的无 dyndbg 基线通过 no-op 编译消除后端实现
- 动态调试对比：同一测试路径在大量调试语句被动态禁用时的 static path 数据
- `ENABLE_LOG=1` 不属于正式对照，只用于功能验证

三次编译 / 三次启动的执行顺序：

1. baseline build：编译期关闭 `dyndbg`，让调用点在编译期直接消失。
2. branch build：关闭 `dyndbg`，开启 `branchdbg`，保留 descriptor + 运行时 branch gate。
3. patch build：默认构建，保留原始 static patch backend。

对应命令示例：

```bash
# 1) baseline build + baseline run
make run_kernel ENABLE_KVM=0 SMP=4 INITRAMFS_SKIP_GZIP=1 LOG_LEVEL=debug SYSCALL_INFO=off NO_DEFAULT_FEATURES=1 FEATURES=cvm_guest

# guest 内只跑 baseline backend
快速档：
ITERS=200 BACKEND_MODE=baseline /test/dyndbg/perf.sh
正式档：
BACKEND_MODE=baseline /test/dyndbg/perf.sh

# 2) branch build + branch run
make run_kernel ENABLE_KVM=0 SMP=4 INITRAMFS_SKIP_GZIP=1 LOG_LEVEL=debug SYSCALL_INFO=off NO_DEFAULT_FEATURES=1 FEATURES="cvm_guest,branchdbg"

# guest 内只跑 branch backend
快速档：
ITERS=200 BACKEND_MODE=branch /test/dyndbg/perf.sh
正式档：
BACKEND_MODE=branch /test/dyndbg/perf.sh

# 3) patch build + static run
make run_kernel ENABLE_KVM=0 SMP=4 INITRAMFS_SKIP_GZIP=1 LOG_LEVEL=debug SYSCALL_INFO=off

# guest 内只跑 patch backend（静态 patch 站点存在，运行时规则禁用）
快速档：
ITERS=200 BACKEND_MODE=static /test/dyndbg/perf.sh
正式档：
BACKEND_MODE=static /test/dyndbg/perf.sh
```

### 6.1.1 Real Workload Supplement (P-01R)

为了避免 P-01 只停留在单次 `log` 的合成循环，进行一组真实文件系统 workload，对比同一内核热点路径在”无规则/低干扰”与”规则存在但动态禁用”下的端到端耗时。当前最合适的 3 个场景是：

| workload | 主要内核路径 | 说明 |
| --- | --- | --- |
| create_delete | `open` / `close` / `unlink` | 小文件创建后立即删除，适合观察 VFS + ext2 的基础路径开销 |
| rename | `rename` | 单路径重命名，适合观察目录项查找和重命名路径 |
| mkdir_rmdir | `mkdir` / `rmdir` | 目录创建/删除，适合观察目录操作路径 |

对应脚本：`/test/dyndbg/workload.sh`

说明：

- baseline、branch、patch 必须分开构建、分开启动。
- baseline build：`dyndbg` feature 关闭，站点在编译期直接移除。
- branch build：`branchdbg` feature 开启，保留 descriptor + branch gate。
- patch build：默认构建，站点存在但运行时 `-p` 禁用。
- `workload.sh` 现在支持三种正式模式：`WORKLOAD_MODE=baseline`、`WORKLOAD_MODE=branch`、`WORKLOAD_MODE=static`。
- 正式测试在三次构建里分别运行对应脚本，在宿主机侧汇总对比。
- static path 通过同一批 syscall 热点文件上的动态 debug 规则触发，例如 `file=open.rs -p`、`file=rename.rs -p`、`file=mkdir.rs -p`、`file=rmdir.rs -p`。
- branch path 与 static path 使用同一组 `-p` 规则，主要用于比较“descriptor + branch gate”相对 patch 的开销。
- 这组测试更接近真实 workload 的端到端效果，适合作为 P-01 的补充，不替代 P-01 的 fast-path 微基准。

结果字段：

- `workload_mode`：`baseline` / `branch` / `static`
- `elapsed_ms`：当前模式下的平均耗时
- `task_clock_ms`：当前模式下的平均任务时钟
- `context_switches`：当前模式下的平均上下文切换数
- `page_faults`：当前模式下的平均页故障数

三向对比：

1. 先在 baseline build 里运行 `WORKLOAD_MODE=baseline`，采 baseline。
2. 再在 branch build 里运行 `WORKLOAD_MODE=branch`，采 branch 组数据。
3. 最后在 patch build 里运行 `WORKLOAD_MODE=static`，采 patch 组数据。

构建示例：

```bash
# baseline build: sites compiled out
# baseline build: no dynamic debug sites (no-site baseline)
# Use NO_DEFAULT_FEATURES to disable default features (which include dyndbg).
make run_kernel ENABLE_KVM=0 SMP=4 INITRAMFS_SKIP_GZIP=1 LOG_LEVEL=debug SYSCALL_INFO=off NO_DEFAULT_FEATURES=1 FEATURES=cvm_guest

# branch-based build: branch conditional instrumentation compiled in
# Enable `branchdbg` feature while leaving `dyndbg` off.
make run_kernel ENABLE_KVM=0 SMP=4 INITRAMFS_SKIP_GZIP=1 LOG_LEVEL=debug SYSCALL_INFO=off NO_DEFAULT_FEATURES=1 FEATURES="cvm_guest,branchdbg"

# patch-based build: static patch sites (original dyndbg)
make run_kernel ENABLE_KVM=0 SMP=4 INITRAMFS_SKIP_GZIP=1 LOG_LEVEL=debug SYSCALL_INFO=off
```

对应 workload 运行命令：

```bash
# baseline build
快速档：
WORKLOAD_MODE=baseline ITERS=10  /test/dyndbg/workload.sh
正式档：
WORKLOAD_MODE=baseline /test/dyndbg/workload.sh

# branch build
快速档：
WORKLOAD_MODE=branch  ITERS=10  /test/dyndbg/workload.sh
正式档：
WORKLOAD_MODE=branch  /test/dyndbg/workload.sh

# patch build
快速档：
WORKLOAD_MODE=static  ITERS=10  /test/dyndbg/workload.sh
正式档：
WORKLOAD_MODE=static  /test/dyndbg/workload.sh
```

---

### 6.2 Batch Patch Transaction Benchmark (P-02)

Scope：

- 测量 runtime rule update 的端到端延迟
- 包括 procfs dispatch、命令解析、selector recompute 和 patch transaction
- 在同一内核、同一次 guest 启动、同一次脚本执行中，对比 `backend=per_site` 与 `backend=batch`
- 该项用于补充说明系统开销来源，不替代 P-01 的性能基线要求

对比维度：

- per-site patch backend
- batch patch backend

记录：patch time、`modules_repatched`、`sites_patched`。

测试脚本：`/test/dyndbg/patch_bench.sh`


```bash
# 快速参数档：
PATCH_ITERS=20 /test/dyndbg/patch_bench.sh
# 正式档：
/test/dyndbg/patch_bench.sh  # 脚本内部自动跑 per_site 和 batch 两轮
```

用途：

- 验证 patch transaction 正常
- 验证 stats 正常
- 验证无死锁/异常
- 验证同一实现下的 per-site 与 batch 后端差异

三种模式：

- `PATCH_MODE=enable` 观察纯 enable 更新
- `PATCH_MODE=clear` 观察纯 clear 更新
- `PATCH_MODE=toggle` 观察 enable + clear 成对更新

默认使用 `PATCH_MODE=toggle`，它最接近当前批量修补事务的常见更新形态（enable + clear 成对操作）。

---

## 7. 并发与稳定性测试

**只有 C 系列需要多核**（并发 patching 是核心测试目标）：

```bash
make run_kernel SMP=4 LOG_LEVEL=debug SYSCALL_INFO=off RELEASE=1 MEM=16G
```

### 7.1 规则切换并发 (C-01)

- CPU0：高频 `mode=log`（经过 NOP5 指令槽的真实热路径）
- CPU1：循环 module=mm +p / clear 规则切换

目标：验证”一边修改指令槽、一边执行经过同一批指令槽的热路径”这一最危险并发场景下的 SMP 安全性（无 panic/oops、无死锁、patch 事务一致性）。

本测试使用 `BENCH_MODE=log`（默认），前台负载经过 `dyndbg_debug!()` 调用点及其 NOP5/JMP 指令槽，与后台的指令修补在同一批内存地址上并发操作，直接验证 SMP 安全修补协议（全局排他锁 + IPI 集结 + CR0.WP 原子写入 + SeqCst 屏障）在极端竞态下的正确性。

测试脚本：`/test/dyndbg/concurrency.sh`

快速参数档：

```bash
# 并发测试：前台 log 模式跑热路径，后台高频 toggle 规则
LOG_ITERS=50 TOGGLE_ITERS=20 /test/dyndbg/concurrency.sh
```
正式档：
```bash
/test/dyndbg/concurrency.sh
```

说明：CSV 会包含 `bench_mode`、`duration_us`、`dmesg_status`、`dmesg_hits`、`status` 等字段；以无 panic/oops 为通过标准。

### 7.2 高频 Patch Stress (C-02)

执行多次 enable/disable，系统保持稳定。

测试脚本：`/test/dyndbg/stress.sh`

快速参数档：

```bash
# 高频压测：启用 dmesg 检查。若需更高置信度，可把 TOGGLE_ITERS 值提高到 500 或以上。
TOGGLE_ITERS=50 /test/dyndbg/stress.sh
```
正式档：
```bash
/test/dyndbg/stress.sh
```

---

### 7.3 Patch Storm (C-03)

**测试目标**：多 CPU 混合规则操作下验证真实场景稳定性。

**测试方法**：

- CPU0: 高频 log
- CPU1: module=mm +p / clear
- CPU2: add/del rule
- CPU3: func/file/line 混合切换

持续运行，验证无死锁/非法指令/崩溃。

测试脚本：`/test/dyndbg/patch_storm.sh`


默认 `BENCH_MODE=log`，前台负载经过 NOP5 指令槽，覆盖真实热路径与修补并发的稳定性。

快速参数档：

```bash
# Patch storm：启用 dmesg 检查，保持较小迭代以便快速验收
BENCH_MODE=log STORM_ITERS=20 LOG_ITERS=50 CLEAR_INTERVAL=10  \
    /test/dyndbg/patch_storm.sh
```

正式档：
```bash
/test/dyndbg/patch_storm.sh
```

---

## 8. 最终测试总结

### 8.1 功能正确性（F 系列 + 扩展验证）

| Case | 结果 | 说明 |
|------|------|------|
| F-01~F-08 | 全部 pass | selector 语义、last-match-wins、动态切换、鲁棒性全部通过 |
| FL-01~08 / M-01~08 | 全部 pass | 输出格式 flags 顺序语义、三通道匹配（精确/段/通配符）与索引等价性 |
| R-01~06 / S-01~07 | 全部 pass | 异常命令鲁棒性、cat 规则链与调试点状态查看 |
| T/H-01~04 | 全部 pass | trace 事件/丢失统计/per-CPU 归属、热点统计一致性 |
| EQ-01~04 | 全部 pass | 增量刷新与全量重放的最终状态等价 |

### 8.2 增量更新与索引消融（I 系列）

| Case | 结果 | 说明 |
|------|------|------|
| I-03 | pass | append 增量刷新恒定 ~2µs 与链长无关（1..5000）；del 全量重放随链长线性（17→58,363µs）；full 基线恒定 65µs |
| I-02 | pass | 四维索引全部受益：line 2.82×、file 8.40×、func 4.66×、module 5.60×；三模式分解 L2 较 L1 加速 5.6×；全部 sites_patched=0 确认无 patching 噪声 |
| 可扩展性 | pass | N=10,000 下 line 118×、func 141×、module 307×；file 平坦 3.4-8.4× |

### 8.3 性能基准（P 系列）

| Case | 结果 | 说明 |
|------|------|------|
| P-01 | pass | baseline=9,386μs < static=9,485μs (+1.1%) < branch=9,594μs (+2.2%)，符合 `baseline ≈ static < branch`；TOST 等价性检验（δ=0.05 ns/call，p<0.001）确认 static 与 baseline 统计等价 |
| P-01R | pass | 6/6 真实 workload 负载通过 TOST（δ=5% baseline）等价性检验 |
| P-02 | pass | batch=9.38s vs per_site=12.41s（快 24%）；SMP 事务 4,000 vs 20,002,000（**5000×**）；20,002,000 站点修补量相同 |

### 8.4 并发稳定性（C 系列）

| Case | 结果 | 说明 |
|------|------|------|
| C-01~C-03 | 全部 pass | 无 panic/oops/死锁，dmesg 干净 |

### 8.5 汇总

| 维度 | 结论 |
|------|------|
| selector 三通道匹配 | ✅ 通过（精确/段/通配符，四维全部索引化） |
| last-match-wins 规则链 | ✅ 通过 |
| runtime 动态切换 | ✅ 通过 |
| static fast path 最小化 | ✅ static 仅比 baseline 高 ~1.1%，TOST 确认统计等价 |
| static patch 生效 | ✅ `baseline ≈ static < branch` 趋势成立 |
| batch patch 生效 | ✅ SMP 事务数降低 5000×，总耗时降低 24% |
| 多维度索引加速候选收集 | ✅ line 2.82×、file 8.40×、func 4.66×、module 5.60×；随规模放大至 118-307× |
| 增量刷新 | ✅ append 恒定 ~2µs 与链长无关；增量与全量状态等价 |
| 输出格式 flags | ✅ +f/+l/+m/+t 前缀与 set/clear/override 顺序语义 |
| trace 追踪与热点统计 | ✅ per-CPU ring 事件/丢失/热点一致 |
| SMP 并发稳定 | ✅ 无 panic/crash/死锁，dmesg 干净 |

---

## 9. 辅助工具与脚本

### 9.1 dyndbg_bench

路径：`/proc/sys/kernel/dyndbg_bench`

写入命令（参数可组合）：

```text
mode=log iters=1000000
backend=batch index=on recompute=incremental mode=log iters=10000000
```

| 参数 | 可选值 | 作用 |
|------|--------|------|
| `backend=` | `batch`（默认）/ `per_site` | 修补策略（运行时热切换，消融实验用） |
| `index=` | `on` / `off` | 开关三通道索引（消融实验用） |
| `recompute=` | `incremental` / `full` | 增量刷新 vs 全量重算（EQ/三模式分解实验用） |
| `mode=` | `log` / `log_batch` | 测试模式（单站点 / 64 站点批量） |
| `iters=` | 任意正整数 | 循环次数 |

读取输出字段：

- last_mode / last_iters / last_duration_us / counter

说明：

- `mode=log` 会触发 `dyndbg_debug!` 调用，用于测量禁用/启用路径；`mode=log_batch` 循环调用 64 个不同调用点（`bench_log_0..63`）。
- 默认 selector 可用 `module=dyndbg_bench`、`file=dyndbg_bench.rs`、`func=bench_log`。
- line selector 由 initramfs 生成的 `/etc/dyndbg_line.txt` 提供；若缺失则回退到稳定默认值。

### 9.2 测试脚本（/test/dyndbg）

脚本位于 guest 内的 `/test/dyndbg/`，在 guest shell 内执行：

```bash
chmod +x /test/dyndbg/*.sh
```

- 功能测试：`/test/dyndbg/functional.sh`（F-01~F-08）
- 输出格式 flags：`/test/dyndbg/flags.sh`（FL-01~08）
- 三通道匹配：`/test/dyndbg/match3.sh`（M-01~08）
- 异常鲁棒性：`/test/dyndbg/robustness.sh`（R-01~06）
- 状态查看：`/test/dyndbg/status.sh`（S-01~07）
- 追踪与热点：`/test/dyndbg/trace.sh`（T/H 系列）
- 性能基线：`/test/dyndbg/perf.sh`（P-01）
- 真实负载：`/test/dyndbg/workload.sh`（P-01R）
- 增量刷新：`/test/dyndbg/incremental.sh`（I-03 + EQ-01~04）
- 索引消融：`/test/dyndbg/index_ablation.sh`（I-02）
- 可扩展性：`/test/dyndbg/scalability.sh`
- Patch 基准：`/test/dyndbg/patch_bench.sh`（P-02）
- 并发压力：`/test/dyndbg/concurrency.sh`（C-01）
- 高频压测：`/test/dyndbg/stress.sh`（C-02）
- Patch 风暴：`/test/dyndbg/patch_storm.sh`（C-03）
- 一键执行：`/test/dyndbg/run_all.sh`

可选环境变量：

- `MODULE_KEY` / `FILE_KEY` / `FUNC_KEY` / `LINE_KEY`
- `ITERS` / `TOGGLE_ITERS` / `LOG_ITERS`
- `ENABLE_LOG` / `RUN_COUNT`
- `RUNS` / `WARMUP` / `CLEAR_INTERVAL` / `SLEEP_US` / `KEEP_STATE`
- `RESULTS_DIR` / `RUN_ID` / `COMMIT` / `PHASE` / `DESCRIPTORS`
- `PERF` / `PERF_EVENTS` / `DMESG_CHECK` / `BYTES_DISABLED` / `BYTES_ENABLED`

run_all.sh 额外变量（仅覆盖同构建用例：F / I-02 / P-01 static / P-01R static / P-02）：

- `RUN_FUNCTIONAL` / `RUN_INDEX_ABLATION`
- `RUN_PERF` / `RUN_WORKLOAD`
- `RUN_PATCH_BENCH`

快速参数档：

```bash
ITERS=10 RUNS=1 INDEX_ITERS=100 PATCH_ITERS=100 /test/dyndbg/run_all.sh
```

说明：

- `run_all.sh` 默认将结果写入 `results/<RUN_ID>/...`。

### 9.3 统计与观测接口

**dyndbg_stats**：路径 `/proc/sys/kernel/dyndbg_stats`

读取输出字段（5 个 AtomicU64 计数器）：

- descriptors_recomputed：累计重算的描述符个数
- modules_repatched：累计触发模块级指令修补的次数
- sites_patched：累计修补的指令槽个数
- patch_transactions：累计执行的修补事务次数
- last_update_latency_us：最近一次规则更新的耗时（微秒，TSC 计时）

重置统计：

```text
reset
```

**dyndbg_trace**：路径 `/proc/sys/kernel/dyndbg_trace` —— trace 事件快照与丢失统计。`cat` 读取时执行一次破坏性快照（从每个 CPU 的 ring buffer 收割上次快照以来新增的事件，按 TSC 合并排序），`events=`/`lost=` 行给出累计事件数与 ring 覆盖丢弃数；`echo reset` 清零。

**dyndbg_hotspots**：路径 `/proc/sys/kernel/dyndbg_hotspots` —— 热点统计。`cat` 读取时输出跨 CPU 求和后命中次数最高的前 10 个调用点；`echo reset` 清零。


### 9.4 结构化输出与结果目录

所有脚本输出统一的结构化结果行：

```text
RESULT key=value key=value ...
```

并写入 CSV（默认目录为 guest 内的 `/ext2/results/`；如果 9P 可用则会切换到 `/results/`）：

```
results/
    functional/results.csv              # F-01~F-08
    flags/results.csv                   # FL-01~08 输出格式 flags
    match3/results.csv                  # M-01~08 三通道匹配
    robustness/results.csv              # R-01~06 异常鲁棒性
    status/results.csv                  # S-01~07 状态查看
    trace/results.csv                   # T/H 追踪与热点
    perf/results.csv                    # P-01 微基准
    workload/results.csv                # P-01R 真实负载
    index_ablation/results.csv          # I-02 索引消融（含 recompute 三模式）
    scalability/N{500..10000}/index_ablation/results.csv   # 可扩展性（按 N 分目录）
    patch_bench/results.csv             # P-02 批量修补对比
    incremental/results.csv             # I-03 增量刷新 + EQ 等价性
    concurrency/results.csv             # C-01
    stress/results.csv                  # C-02
    patch_storm/results.csv             # C-03
```

每次测试前设置：

```text
RUN_ID=<custom-id>
```

说明：

- 如果 perf/dmesg/字节信息不可用，对应字段会记录为 `na` 或 `unknown`。
- 由于当前内核不支持 9P，共享结果目录会回退为 ext2 镜像；如需把结果同步到宿主机源码树下的 `results/`，在 guest 退出后运行 `tools/dyndbg/collect_results.sh`。
- `functional.sh` 的结构化结果会使用 `status=pass|fail`，并附带 `expected` / `actual`，不再只表示 `executed`。